다형성
#### Operation overriding
- `Subclass` 가 `Superclass` 함수의 `prototype` 과 동일한 함수를 재정의하는 것

#### Dynamic binding
- runtime 시 포인터 변수가 실제 가리키는 객체 타입을 기준으로 호출할 함수를 결정함
	- `virtual` 키워드 사용
	- [[Virtual function]]
