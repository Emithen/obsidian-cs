
#### curse of dimensionality
데이터가 고차원이 될 수록 연산 복잡도는 감당이 안 되는 수준으로 올라간다
그런데 현실의 데이터는 대부분 매우 고차원이다

#### principal component analysis

#### ML
x -> f(x) -> y 를 잘 예측하는 f(x) 를 찾는 과정

#### Loss function
- L1 loss - 절댓값
- L2 loss - 제곱


#### Image Classification
- one-hot class vector

